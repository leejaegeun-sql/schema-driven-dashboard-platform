# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/
env/
ENV/
cleanenv/

# Browser / verification artifacts (screenshots kept locally, not packaged;
# add a documentation image deliberately with `git add -f` if one is ever wanted)
*.png
*.jpg
*.jpeg
*.pdf
screenshots/
chrome-profile*/
.chrome-user-data/

# Personal application material: CV, Q&A answers, the assignment brief.
# Never part of the submission -- the directory is ignored wholesale so a new
# file dropped in it cannot be committed by accident.
application_docs/

# Built submission archive
*.zip

# Editors / OS
.idea/
.vscode/
.DS_Store
